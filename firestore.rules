rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // All application access is server-side through service identities.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
